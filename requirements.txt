<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Assinar Relatório de Viagem</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;1,300;1,400&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet" />
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --ink:     #1a1714;
    --paper:   #f7f4ef;
    --cream:   #ede9e0;
    --rust:    #8b4a2f;
    --rust-lt: #b5704f;
    --line:    #c9c1b0;
    --shadow:  rgba(26,23,20,.10);
  }

  html, body {
    height: 100%;
    background: var(--paper);
    color: var(--ink);
    font-family: 'DM Sans', sans-serif;
    font-weight: 300;
  }

  /* ── background texture ── */
  body::before {
    content: '';
    position: fixed; inset: 0; z-index: 0;
    background-image:
      radial-gradient(ellipse 80% 60% at 15% 10%, #e8dfd0 0%, transparent 60%),
      radial-gradient(ellipse 60% 80% at 85% 90%, #ddd5c5 0%, transparent 55%);
    pointer-events: none;
  }

  /* ── layout ── */
  .page {
    position: relative; z-index: 1;
    min-height: 100vh;
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    padding: 3rem 1.5rem;
    gap: 0;
  }

  /* ── header ── */
  header {
    text-align: center;
    margin-bottom: 3.5rem;
  }

  .kicker {
    font-family: 'DM Sans', sans-serif;
    font-size: .68rem;
    font-weight: 500;
    letter-spacing: .22em;
    text-transform: uppercase;
    color: var(--rust);
    margin-bottom: 1rem;
  }

  h1 {
    font-family: 'Cormorant Garamond', serif;
    font-weight: 300;
    font-size: clamp(2.2rem, 5vw, 3.4rem);
    line-height: 1.1;
    letter-spacing: -.01em;
    color: var(--ink);
  }
  h1 em {
    font-style: italic;
    color: var(--rust);
  }

  .subtitle {
    margin-top: .9rem;
    font-size: .88rem;
    color: #6b6358;
    letter-spacing: .02em;
  }

  /* ── card ── */
  .card {
    background: #fff;
    border: 1px solid var(--line);
    border-radius: 2px;
    box-shadow: 0 2px 6px var(--shadow), 0 12px 40px var(--shadow);
    padding: 2.8rem 3rem;
    width: 100%;
    max-width: 520px;
  }

  /* ── drop zone ── */
  .dropzone {
    border: 1.5px dashed var(--line);
    border-radius: 2px;
    padding: 2.8rem 2rem;
    text-align: center;
    cursor: pointer;
    transition: border-color .2s, background .2s;
    background: var(--paper);
    position: relative;
  }
  .dropzone:hover, .dropzone.over {
    border-color: var(--rust-lt);
    background: #fdf8f4;
  }
  .dropzone input[type="file"] {
    position: absolute; inset: 0; opacity: 0; cursor: pointer; width: 100%; height: 100%;
  }

  .drop-icon {
    width: 44px; height: 44px;
    margin: 0 auto 1rem;
    opacity: .45;
  }

  .drop-label {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.15rem;
    font-weight: 400;
    color: var(--ink);
    margin-bottom: .35rem;
  }
  .drop-sub {
    font-size: .78rem;
    color: #9b9188;
    letter-spacing: .04em;
  }

  /* selected file badge */
  .file-badge {
    display: none;
    align-items: center;
    gap: .7rem;
    margin-top: 1.4rem;
    padding: .75rem 1rem;
    background: var(--cream);
    border-radius: 2px;
    border: 1px solid var(--line);
  }
  .file-badge.visible { display: flex; }
  .file-badge svg { flex-shrink: 0; color: var(--rust); }
  .file-badge span {
    font-size: .82rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    color: var(--ink);
  }
  .file-badge .remove-btn {
    margin-left: auto;
    background: none; border: none; cursor: pointer;
    color: #9b9188; font-size: 1rem; line-height: 1;
    padding: 0 .2rem;
    transition: color .15s;
    flex-shrink: 0;
  }
  .file-badge .remove-btn:hover { color: var(--rust); }

  /* ── button ── */
  .btn {
    margin-top: 1.8rem;
    width: 100%;
    padding: .95rem 1.5rem;
    background: var(--rust);
    color: #fff;
    border: none;
    border-radius: 2px;
    font-family: 'DM Sans', sans-serif;
    font-size: .85rem;
    font-weight: 500;
    letter-spacing: .1em;
    text-transform: uppercase;
    cursor: pointer;
    transition: background .2s, transform .1s;
    display: flex; align-items: center; justify-content: center; gap: .6rem;
  }
  .btn:hover:not(:disabled) { background: #7a3f27; }
  .btn:active:not(:disabled) { transform: scale(.99); }
  .btn:disabled { background: var(--line); cursor: not-allowed; }

  /* spinner */
  .spinner {
    width: 16px; height: 16px;
    border: 2px solid rgba(255,255,255,.35);
    border-top-color: #fff;
    border-radius: 50%;
    animation: spin .7s linear infinite;
    display: none;
  }
  .btn.loading .spinner { display: block; }
  .btn.loading .btn-text { opacity: .7; }
  @keyframes spin { to { transform: rotate(360deg); } }

  /* ── status messages ── */
  .status {
    margin-top: 1.2rem;
    padding: .8rem 1rem;
    border-radius: 2px;
    font-size: .82rem;
    display: none;
    align-items: center;
    gap: .6rem;
  }
  .status.visible { display: flex; }
  .status.success {
    background: #f0faf4;
    border: 1px solid #a8d5b5;
    color: #2e6645;
  }
  .status.error {
    background: #fff4f2;
    border: 1px solid #e8b4a8;
    color: #8b2e1f;
  }

  /* ── divider ── */
  .divider {
    width: 40px; height: 1px;
    background: var(--line);
    margin: 3rem auto 0;
  }

  /* ── footer ── */
  footer {
    margin-top: 1.4rem;
    font-size: .72rem;
    color: #aaa49b;
    letter-spacing: .06em;
    text-transform: uppercase;
    text-align: center;
  }

  @media (max-width: 560px) {
    .card { padding: 2rem 1.5rem; }
  }
</style>
</head>
<body>

<div class="page">
  <header>
    <p class="kicker">Instituto Mamirauá</p>
    <h1>Assinar<br><em>Relatório de Viagem</em></h1>
    <p class="subtitle">Envie o PDF e receba o documento assinado automaticamente.</p>
  </header>

  <div class="card">
    <!-- Drop zone -->
    <div class="dropzone" id="dropzone">
      <input type="file" id="fileInput" accept=".pdf" />

      <svg class="drop-icon" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M12 40h24a2 2 0 0 0 2-2V18L28 8H12a2 2 0 0 0-2 2v28a2 2 0 0 0 2 2Z" stroke="#8b4a2f" stroke-width="1.5" stroke-linejoin="round"/>
        <path d="M28 8v10h10" stroke="#8b4a2f" stroke-width="1.5" stroke-linejoin="round"/>
        <path d="M24 22v12M20 30l4 4 4-4" stroke="#8b4a2f" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>

      <p class="drop-label">Arraste o PDF aqui</p>
      <p class="drop-sub">ou clique para selecionar o arquivo</p>
    </div>

    <!-- Selected file badge -->
    <div class="file-badge" id="fileBadge">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
        <polyline points="14 2 14 8 20 8"/>
      </svg>
      <span id="fileName">—</span>
      <button class="remove-btn" id="removeBtn" title="Remover">✕</button>
    </div>

    <!-- Submit button -->
    <button class="btn" id="submitBtn" disabled>
      <div class="spinner"></div>
      <span class="btn-text">Assinar documento</span>
    </button>

    <!-- Status -->
    <div class="status" id="statusMsg">
      <span id="statusText"></span>
    </div>
  </div>

  <div class="divider"></div>
  <footer>Assinatura automática · Ana Paula Soares Farias</footer>
</div>

<script>
  const dropzone   = document.getElementById('dropzone');
  const fileInput  = document.getElementById('fileInput');
  const fileBadge  = document.getElementById('fileBadge');
  const fileName   = document.getElementById('fileName');
  const removeBtn  = document.getElementById('removeBtn');
  const submitBtn  = document.getElementById('submitBtn');
  const statusMsg  = document.getElementById('statusMsg');
  const statusText = document.getElementById('statusText');

  let selectedFile = null;

  function setFile(file) {
    if (!file || !file.name.toLowerCase().endsWith('.pdf')) {
      showStatus('error', 'Por favor, selecione um arquivo PDF válido.');
      return;
    }
    selectedFile = file;
    fileName.textContent = file.name;
    fileBadge.classList.add('visible');
    submitBtn.disabled = false;
    clearStatus();
  }

  function clearFile() {
    selectedFile = null;
    fileInput.value = '';
    fileBadge.classList.remove('visible');
    submitBtn.disabled = true;
    clearStatus();
  }

  function showStatus(type, msg) {
    statusMsg.className = `status visible ${type}`;
    statusText.textContent = msg;
  }

  function clearStatus() {
    statusMsg.className = 'status';
  }

  // File input change
  fileInput.addEventListener('change', () => {
    if (fileInput.files[0]) setFile(fileInput.files[0]);
  });

  // Remove file
  removeBtn.addEventListener('click', clearFile);

  // Drag & drop
  dropzone.addEventListener('dragover', e => { e.preventDefault(); dropzone.classList.add('over'); });
  dropzone.addEventListener('dragleave', () => dropzone.classList.remove('over'));
  dropzone.addEventListener('drop', e => {
    e.preventDefault();
    dropzone.classList.remove('over');
    const file = e.dataTransfer.files[0];
    if (file) setFile(file);
  });

  // Submit
  submitBtn.addEventListener('click', async () => {
    if (!selectedFile) return;

    submitBtn.classList.add('loading');
    submitBtn.disabled = true;
    clearStatus();

    const formData = new FormData();
    formData.append('pdf', selectedFile);

    try {
      const res = await fetch('/assinar', { method: 'POST', body: formData });

      if (res.ok) {
        const blob = await res.blob();
        const stem = selectedFile.name.replace(/\.pdf$/i, '');
        const url  = URL.createObjectURL(blob);
        const a    = document.createElement('a');
        a.href = url;
        a.download = `${stem}_assinado.pdf`;
        document.body.appendChild(a);
        a.click();
        a.remove();
        URL.revokeObjectURL(url);
        showStatus('success', '✓ Documento assinado e baixado com sucesso!');
        clearFile();
      } else {
        const data = await res.json().catch(() => ({}));
        showStatus('error', data.erro || 'Erro ao assinar o documento. Tente novamente.');
      }
    } catch {
      showStatus('error', 'Não foi possível conectar ao servidor.');
    } finally {
      submitBtn.classList.remove('loading');
      submitBtn.disabled = false;
    }
  });
</script>
</body>
</html>
